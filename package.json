{
  "dependencies": {
    "@lit/task": "^1.0.2",
    "@sabaki/sgf": "^3.4.7",
    "lit": "^3.3.0"
  },
  "scripts": {
    "build": "bun build --outdir static --sourcemap=linked --splitting frontend/index.ts",
    "watch": "bun run build --watch"
  }
}
